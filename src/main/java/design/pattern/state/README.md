	状态模式主要是把一些判断分支以及分支处理放置到了State模式中；通过模式顺序性来逐个判断当前状态属于哪一个，然后才去相应的行动；形成一条状态链。
	在实现过程中按照联调的顺序，逐个判断，如果不符合，则设置状态为状态链中下一个状态链，再进行匹配。
	这样对于调用方屏蔽了其内部的细节，初始化的时候只要把最初的状态初始化好，放置到入口类（Work）中即可。

	在看Bob大叔的State的例子于此同出一辙：设置一个开关，打开动作执行完成后，将会把状态设置为关闭；如果action再此触发，将会触发关的动作，然后设置状态为open，这样下次将会执行开的动作，设置状态为关；如此反复。